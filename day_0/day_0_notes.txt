React js is a javascript library for creating effective user interface. Some Examples for the beginning steps to learn how to establish the react app
and how to add components we going to learn by doing.



1. how to create React app
--we will use a command in terminal "npx create-react-app app-name"
--"cd app-name"
--"start npm"

2.components
--header
--side nav
--footer
--main content
--root component (all component inside one)

--components are reusable to inherit other properties
--StateLess functional component and stateful class component
--StateLess functional components are javascript functions which return html which describes the UI
--Stateful class component are extending react component class with render method returning html 

3.